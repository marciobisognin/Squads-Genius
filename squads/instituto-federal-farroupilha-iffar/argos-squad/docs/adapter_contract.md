# Contrato AdapterDiario

Todo olho implementa `listar_edicoes`, `obter_publicacoes` e `healthcheck`, declara `codigo`, `dominios_permitidos` e `rate_limit_rpm`, acompanha fixtures e passa por homologação quando for fonte nova.
