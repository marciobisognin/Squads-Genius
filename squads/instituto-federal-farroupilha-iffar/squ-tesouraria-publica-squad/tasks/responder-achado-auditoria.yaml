id: responder-achado-auditoria
description: Estruturar resposta a achado TCU/CGU com evidências e revisão obrigatória.
assigned_agent: auditor-tcu-cgu
dependencies:
- montar-dossie-prestacao-contas
input_schema:
  type: object
  required:
  - source
  - reference_date
  properties:
    source:
      type: string
    reference_date:
      type: string
    records:
      type: array
    human_review_required:
      type: boolean
  additionalProperties: true
  title: responder-achado-auditoria input
output_schema:
  type: object
  required:
  - status
  - human_review_required
  properties:
    status:
      type: string
    human_review_required:
      const: true
    issues:
      type: array
    outputs:
      type: array
validation_rules:
- validar schema de entrada
- registrar origem e data de referência
- marcar campos ausentes como dado_incompleto
- não emitir decisão final
timeout: 120s
retry_policy:
  max_attempts: 2
  retry_on:
  - erro de parsing
  - arquivo temporariamente indisponível
  do_not_retry_on:
  - schema inválido
  - ausência de dado obrigatório
human_approval: true
failure_behavior: Gerar relatório de bloqueio com dados faltantes, sem fabricar evidências ou valores.
quality_criteria:
- Mapear achados por origem, normativo citado, prazo, status e unidade responsável.
- Estruturar matriz achado x resposta x evidência x plano de ação.
- saída auditável e revisável
