# Advanced Release Notes — v1.1.0-maeve

- Preserva o squad original Nirvana README Architect.
- Adiciona comando universal `*exit` em todos os agentes.
- Adiciona CLI operacional `nirvana-readme-architect`.
- Adiciona `scripts/readme-architect.cjs` para scan/generate/validate de README.
- Adiciona `scripts/smoke-test.cjs` e `npm run smoke`.
- Adiciona `package.json` e wrapper em `bin/` para instalação global via npm.
- Atualiza `squad.yaml` para declarar scripts reais.
